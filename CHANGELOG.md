# 更新历史

本文档按日期记录用户可见功能、行为变化、修复和文档变化。项目仍处于早期快速迭代阶段，版本号以 `package.json` 为准。

## v0.3.0 - 2026-08-02

### 破坏性变更

- 删除旧 Guide/Edit 界面、提示词、通用 JSON Patch、路径白名单兼容层、字段局部预览和直接写字段流程；不再提供兼容或回退实现。
- 首次打开新版 Agent 历史库时，不可恢复地删除应用数据目录中精确匹配的 `ai_chat_history.sqlite3`、`-wal` 和 `-shm`，并创建全新的 `agent_history.sqlite3`；角色卡文件和草稿不受影响。
- 旧设置中的明文 API key 不再读取或自动迁移，系统凭据库是唯一密钥来源。

### 新增

- 引入由前端创建的 `AgentPermission`，支持整卡、区块、精确字段、整个世界书和单个条目 scope，并分别控制读取、编辑和注入能力。
- 卡片编辑、已有世界书条目编辑和世界书候选注入改用三种受类型约束的语义工具；模型不能扩大权限或删除内容。
- 新增世界书候选审阅卡，展示关键词、内容摘要、触发/注入设置、Token 估算和校验错误，支持勾选后原子注入。
- 提案应用增加完整卡片哈希、revision 和世界书条目指纹校验；冲突、越权或任一所选候选无效时整批拒绝。

### 变更

- 主输入框、字段助手和世界书入口统一使用 Agent Studio 的同一 Controller、会话记录和提案队列。
- Agent 会话在首轮成功完成后使用当前模型生成简短标题；标题作为独立历史元数据保存，不进入主对话、工具调用或提案队列。
- `@` 目标成为真实权限范围；未指定目标时使用显式整卡范围，未知或不唯一目标不再静默放宽权限。
- 精简 Agent 设置，仅保留实际运行所需的提供商、端点、模型、凭据、上下文、输出、超时、温度、thinking level、工具能力和 HTTP 策略。
- 更新 README、使用指南、Agent 提示词契约和 AI 知识库，记录 CardForge 设计参考边界与新版确认注入流程。

### 修复

- 修复会话列表用长消息摘要代替名称的问题；列表现在只显示持久化标题，历史事件不会再覆盖已生成标题。
- 修复部分酒馆版本无法识别本项目 PNG 卡的问题；PNG 现在固定先嵌入 CCv2 `chara`、再嵌入 CCv3 `ccv3`，清理旧块后回读校验双份数据再写盘。
- 修复 Agent 重新生成和编辑重发错误复用上一轮权限的问题；现在会按输入区当前选择重新授权，并在运行状态中显示实际权限范围。
- 收窄 Agent 主输入框的橙色键盘焦点高亮，改为 1px 内收轮廓，避免大文本区出现过重的外扩边框。
- 修复世界书折叠条目被通用 flex 样式挤压的问题；条目名称、关键词、状态和两行内容摘要现在按主次分区显示，窄栏下自动堆叠。
- PNG 导出包含内嵌世界书时，状态栏现在提示在 SillyTavern 角色面板中执行“导入卡片世界书”，避免把未自动创建和绑定误认为导出丢失。

## v0.2.0 - 2026-07-31

### 修复

- 修复重新生成和编辑重发在模型请求失败或中断时错误显示“Agent 运行完成”的问题。
- 失败的 Agent 消息现在显示实际错误原因，空消息不再伪装成“正在读取卡片信息”加载状态。
- 修复 Agent 会话恢复时重复拼接缺少 `tool_call_id` 的工具结果，避免重启或切换会话后下一次生成返回 400。

### 新增

- Agent Studio 成为默认入口，提供卡片级会话、工具轨迹、提案审核和三栏编辑台。
- 引入 Pi Agent Core 0.83.0 与 Pi AI 0.83.0；Agent 只读取卡片领域数据并创建待审核提案。
- 增加 workspace、cardRevision、Agent 会话、消息和提案的 SQLite 持久化与旧历史只读迁移。
- 增加 Rust HTTP/SSE 代理、请求取消、HTTPS/loopback 策略和系统凭据库管理。

### 变更

- DeepSeek 与 OpenAI-compatible Chat Completions 统一通过可注入 fetch 接入，模型网络不再由 WebView 直连。
- Guide/Edit 双模式、旧 AI 抽屉和模型直接返回 JSON 的生产流程移除；字段级 AI 操作统一生成同一种提案。
- Node 基线升级到 22.19.0，应用版本升级到 0.2.0。

### 界面

- 修正 Agent Studio 右侧编辑台在设置、预览和项目面板中的窄栏溢出；编辑台内容现在独立滚动，项目操作按可用宽度切换为两列或单列。
- 统一卡片工坊的 Workbench / Iron / Line / Paper / Copper / Sage 表面令牌，移除生产壳层中的紫青渐变、玻璃模糊和重阴影。
- 增加编辑台分隔器的拖拽与方向键调宽，并为窄窗口覆盖层补充遮罩、Escape 关闭和焦点回归。
- 合并同一 Agent turn 中的多段助手文本，避免工具循环把一次回复显示成多个 Agent 气泡；工具结果改为默认折叠，并在展开时格式化 JSON。
- 接通 PI 的 `streamingMessage` 到 Agent Studio，助手气泡会随 SSE 增量更新；消息历史区域限制为纵向滚动并对长内容断词。
- Agent 用户/助手消息增加安全 Markdown 渲染，支持标题、列表、表格、引用、代码块和常用行内格式。
- 修复窄窗口下全局最小宽度导致的横向溢出和截图边界偏大的问题；消息气泡现在按内容收缩并限制在可用宽度内。
- 修正右侧“待审核修改”混入已应用/已丢弃提案的问题，并将提案状态改为中文；Agent 新建提案时默认使用简体中文摘要。
- 修复跨多张角色卡使用 Agent 后左侧看不到会话历史的问题；消息记录现在按角色卡分组，每组默认显示最近 5 条，并可独立展开更早记录。
- 修正最大化窗口下用户/Agent 消息气泡都贴在左侧的问题；现在用户气泡靠右、Agent 气泡靠左，同时保留内容宽度上限。
- 为最后一条 Agent 消息增加“重新生成”，支持中断/未应用直接重跑，以及已应用提案先回退再重跑；最后一条用户消息支持编辑后重新发送。分支替换采用追加式历史和 SQLite 分支标记，不删除原始消息。

### 安全与兼容

- API key 不再写入 localStorage、设置 JSON、SQLite、事件或错误信息；旧 key 仅执行一次迁移。
- 用户确认前卡片和文件不发生修改；冲突提案禁止强制覆盖。
- JSON、PNG/APNG、CHARX、校验、Token 统计和现有编辑器继续保留。

## v0.1.2 - 2026-06-08

### 新增

- 新增安装版自动更新：启动时检查 GitHub Release 的 updater 清单，发现新版本后可确认下载、安装并重启。
- 新增源码/dev 版本提醒：检测到 GitHub 上有更新时只提示拉取源码并重新构建，不会自动修改本地 Git 仓库。
- 新增设置页更新控制，可关闭启动自动检查或手动检查更新。
- 新增 GitHub Actions 发布流程，推送 `v*.*.*` SemVer tag 后构建 Windows 安装包并上传 updater 所需的 `latest.json`。

### 变更

- 自动更新发布版本改用合法三段 SemVer，例如 `v0.1.2`；四段 tag 不再用于 updater。

### 修复

- 修复带有大体积内嵌封面 `data:image/...` 资源的卡片导出后无法导入 SillyTavern 的问题；导出 JSON/PNG/CHARX 时会从卡片元数据中移除内嵌图片资源，PNG 仍使用封面作为图片本体。
- 修复上传封面后保存会让资源页变空的问题；导出文件仍会移除内嵌图片元数据，但编辑器草稿会保留封面资源用于预览和后续保存。
- 修复部分带有 `caBX` / C2PA 来源信息块的封面 PNG 导出后仍被 SillyTavern 判为无效或损坏的问题；导出角色卡 PNG 时会移除该类私有来源块。
- 修复 AI 模型选择模式问题，模型列表选择和手动输入模式现在能按设置正确切换。

## v0.1.0.2 - 2026-06-08

### 修复

- 修复在开场白页将备用开场白设为第一条消息时，原第一条消息没有回到备用开场白列表的问题；现在该操作会交换两者，避免备用列表项被直接移除并减少 AI 字段目标索引失效。

## 2026-06-07

### 改进

- 将仓库历史回退到稳定基线，并保留当前可用的 AI Guide / Edit、JSON patch 预览、字段级 AI、快捷工作流和 Token 统计能力。
- 补回公开 README、完整使用指南和更新历史，文档内容与当前代码能力保持一致。
- 记录当前版本专注于本地角色卡编辑与可确认的 AI JSON patch 预览。

### 修复

- 修复在 Token 统计等长页面滚动后切换到其他页面时，主工作区滚动位置被继承的问题；各页面现在保留自己的滚动位置，未访问页面默认从顶部开始。
- 修复最近文件路径失效时仍反复触发未保存确认并显示原始文件系统错误的问题；失效记录现在会自动从最近列表移除并显示清晰提示。

## 2026-06-06

### 新增

- 新增 OpenAI 兼容 AI 设置、连接测试、模型获取和全局 AI 对话抽屉。
- 新增 AI Edit 结构化 JSON patch 预览、diff 审核、应用/丢弃流程和历史预览重新注入。
- 新增字段级 AI 助手，支持提示词、开场白和世界书等长文本字段的局部润色、扩写、重写、补全、缩短、翻译、冲突检查、关键词提取和候选生成。
- 新增 `@` 编辑目标，用于限定 AI 修改基础信息、提示词、开场白、世界书、具体世界书条目、当前字段、选中文本、空字段、弱字段或校验错误相关字段。
- 新增世界书高级设置编辑，包括插入位置、深度、概率、常驻、选择性触发、向量相似、递归、分组和匹配范围等常见 SillyTavern 选项。
- 新增 Token 统计页，展示总 token、分区统计、最大字段、开场白 prompt preview、世界书条目占用和资源引用统计。

### 改进

- AI 设置默认面向 DeepSeek/OpenAI 兼容接口，并支持推理显示、思考模式、输出长度和超时配置。
- 导入旧卡时增强 V1/V2 到 CCv3 的迁移与未知字段保留。
- PNG/APNG 和 CHARX 导入导出流程完善，导出前会进行角色卡校验和时间戳更新。
- 顶部保存、项目页保存、`Ctrl/Cmd+S` 和右键菜单保存共用同一保存逻辑。已绑定文件优先写回原路径，本地草稿才打开保存对话框。
- 世界书条目支持折叠懒加载、轻量摘要和拖拽/按钮调整显示顺序。
- AI 工作流菜单移入输入区底部 `+` 菜单和 `/` 命令；选择工作流后显示为可移除标签，默认指令不会直接插入输入框。

### 修复

- 修复 AI 聊天生成卡住后难以恢复的问题。
- 修复工作流默认提示词进入输入框造成编辑不便的问题。
- 改进 SillyTavern 世界书标题/备忘录兼容性：导出时优先使用 `comment`，并保留常见高级设置到 `entry.extensions.*`。

## 2026-06-05

### 初始版本

- 创建 Tauri v2、React、Vite、TypeScript 和 Rust 桌面应用骨架。
- 实现 CCv3 角色卡基础编辑、提示词、开场白、世界书、资源、预览和校验面板。
- 支持 JSON、PNG/APNG 和 CHARX 的基础导入导出。
- 实现前端与 Rust 后端的角色卡 schema、迁移和校验基础逻辑。
